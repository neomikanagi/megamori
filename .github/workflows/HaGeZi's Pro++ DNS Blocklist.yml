name: HaGeZi's Pro++ DNS Blocklist (Final Fix)

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */12 * * *'

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  install-and-compile:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install Sing-Box
      run: |
        bash <(curl -fsSL https://sing-box.app/deb-install.sh)

    # 1. 下载 HaGeZi 官方提供的 Sing-box 专用 JSON 文件
    # 相比 TXT，JSON 格式不仅包含规则，还包含了 metadata，兼容性 100%
    - name: Download HaGeZi Pro Plus (JSON)
      run: |
        echo "⬇️ 正在下载官方 Sing-box JSON 规则..."
        curl -fSL -o hagezi_pro_plus.json https://raw.githubusercontent.com/hagezi/dns-blocklists/main/singbox/pro.plus.json
        
        # 检查下载是否成功
        ls -lh hagezi_pro_plus.json
        filesize=$(wc -c <"hagezi_pro_plus.json")
        if [ $filesize -lt 102400 ]; then
          echo "❌ 下载失败：JSON 文件过小"
          exit 1
        fi

    # 2. 使用 compile 命令编译 JSON (这是最稳的方法)
    - name: Compile JSON to SRS
      run: |
        echo "🔨 正在编译 (Compile) JSON -> SRS ..."
        # 注意：这里用的是 compile，不是 convert
        sing-box rule-set compile --output hagezi_pro_plus_adblock.srs hagezi_pro_plus.json
        
        # 检查结果
        echo "📊 编译结果："
        ls -lh hagezi_pro_plus_adblock.srs
        
        srs_size=$(wc -c <"hagezi_pro_plus_adblock.srs")
        # 正常的 srs 文件应该有几 MB
        if [ $srs_size -lt 10240 ]; then
          echo "❌ 编译失败：生成的 SRS 文件几乎是空的 ($srs_size bytes)"
          exit 1
        fi
        echo "✅ 编译成功！"

    - name: Commit and Push
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add hagezi_pro_plus_adblock.srs
        
        if git diff --cached --quiet; then
          echo "⚠️ 规则无变化，跳过提交"
        else
          git commit -m "Auto-update: HaGeZi Pro++ (JSON Source)"
          git pull --rebase origin main
          git push
        fi
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Clean up
      run: |
        rm -f hagezi_pro_plus.json
