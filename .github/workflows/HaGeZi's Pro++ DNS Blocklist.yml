name: HaGeZi's Pro++ (Sing-box v1.11.4)

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */12 * * *'

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  install-and-compile:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    # 1. 强制安装旧版本 v1.11.4
    # 你的直觉是对的，新版本可能对 AdGuard 格式解析有 Bug
    - name: Install Sing-Box v1.11.4
      run: |
        echo "⬇️ 正在安装 Sing-box v1.11.4 ..."
        # 手动下载指定版本的 deb 包
        wget https://github.com/SagerNet/sing-box/releases/download/v1.11.4/sing-box_1.11.4_linux_amd64.deb
        sudo dpkg -i sing-box_1.11.4_linux_amd64.deb
        
        # 验证版本
        sing-box version

    # 2. 下载 HaGeZi Pro Plus (Adblock 格式)
    - name: Download HaGeZi Pro Plus
      run: |
        echo "⬇️ 正在下载规则..."
        curl -fSL -o hagezi_pro_plus_adblock.txt https://raw.githubusercontent.com/hagezi/dns-blocklists/main/adblock/pro.plus.txt
        
        # 安全检查
        filesize=$(wc -c <"hagezi_pro_plus_adblock.txt")
        if [ $filesize -lt 102400 ]; then
          echo "❌ 下载失败：文件太小"
          exit 1
        fi

    # 3. 转换 (使用旧版本 Sing-box)
    - name: Convert to SRS
      run: |
        echo "🔄 正在使用 Sing-box v1.11.4 转换..."
        sing-box rule-set convert --type adguard --output hagezi_pro_plus_adblock.srs hagezi_pro_plus_adblock.txt
        
        echo "📊 转换结果检查："
        ls -lh hagezi_pro_plus_adblock.srs
        
        srs_size=$(wc -c <"hagezi_pro_plus_adblock.srs")
        # 如果还是 21 字节，那就是命了
        if [ $srs_size -lt 1024 ]; then
          echo "❌ 转换失败：哪怕用 v1.11.4 生成的文件依然是空的 ($srs_size bytes)"
          exit 1
        fi
        echo "✅ 转换成功！看来确实是新版本的锅。"

    - name: Commit and Push
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add hagezi_pro_plus_adblock.srs
        
        if git diff --cached --quiet; then
          echo "⚠️ 规则无变化"
        else
          git commit -m "Auto-update: HaGeZi Pro++ (Sing-box v1.11.4)"
          git pull --rebase origin main
          git push
        fi
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Clean up
      run: |
        rm -f hagezi_pro_plus_adblock.txt sing-box_1.11.4_linux_amd64.deb
