name: HaGeZi's Pro++ DNS Blocklist (Final)

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */12 * * *'

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  install-and-compile:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install Sing-Box
      run: |
        bash <(curl -fsSL https://sing-box.app/deb-install.sh)

    # 1. 下载纯域名格式 (Domains Format)
    # 这种格式最干净，只有域名，没有干扰 Sing-box 的特殊符号
    - name: Download HaGeZi Pro Plus (Domains)
      run: |
        echo "⬇️ 正在下载纯域名列表..."
        # 这是 HaGeZi 官方的 Domains 格式链接
        curl -fSL -o pro.plus.txt https://raw.githubusercontent.com/hagezi/dns-blocklists/main/domains/pro.plus.txt
        
        ls -lh pro.plus.txt
        # 简单检查大小
        if [ $(wc -c <"pro.plus.txt") -lt 102400 ]; then
          echo "❌ 下载失败：文件过小"
          exit 1
        fi

    # 2. 使用 Python 将纯域名列表转换为 Sing-box JSON
    # 这是最关键的一步，手动构造 JSON，100% 可控
    - name: Convert to Sing-box JSON
      run: |
        python3 -c '
        import json
        
        input_file = "pro.plus.txt"
        output_file = "hagezi_pro_plus.json"
        
        domains = []
        
        with open(input_file, "r") as f:
            for line in f:
                line = line.strip()
                # 忽略注释和空行
                if not line or line.startswith("#"):
                    continue
                domains.append(line)
        
        print(f"✅ 提取到 {len(domains)} 个域名")
        
        # 构造 Sing-box 规则结构
        data = {
            "version": 1,
            "rules": [
                {
                    "domain": domains
                }
            ]
        }
        
        with open(output_file, "w") as f:
            json.dump(data, f)
        '

    # 3. 编译 JSON 为 SRS
    - name: Compile to SRS
      run: |
        echo "🔨 正在编译..."
        sing-box rule-set compile --output hagezi_pro_plus_adblock.srs hagezi_pro_plus.json
        
        echo "📊 结果检查："
        ls -lh hagezi_pro_plus_adblock.srs
        
        # 确保文件大小正常 (几MB)
        if [ $(wc -c <"hagezi_pro_plus_adblock.srs") -lt 102400 ]; then
          echo "❌ 编译失败：文件依然太小"
          exit 1
        fi

    - name: Commit and Push
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add hagezi_pro_plus_adblock.srs
        
        if git diff --cached --quiet; then
          echo "⚠️ 规则无变化"
        else
          git commit -m "Auto-update: HaGeZi Pro++"
          git pull --rebase origin main
          git push
        fi
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Clean up
      run: |
        rm -f pro.plus.txt hagezi_pro_plus.json
