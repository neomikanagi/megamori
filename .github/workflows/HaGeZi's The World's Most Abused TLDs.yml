name: HaGeZi's Spam TLDs (Fixed)

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */12 * * *'

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  install-and-compile:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    # 1. 安装稳健的 v1.11.4 版本
    - name: Install Sing-Box v1.11.4
      run: |
        echo "⬇️ 正在安装 Sing-box v1.11.4 ..."
        wget https://github.com/SagerNet/sing-box/releases/download/v1.11.4/sing-box_1.11.4_linux_amd64.deb
        sudo dpkg -i sing-box_1.11.4_linux_amd64.deb
        sing-box version

    # 2. 下载规则 (使用 GitHub Raw 源更稳定)
    - name: Download Filter
      run: |
        echo "⬇️ 正在下载规则..."
        curl -fSL -o hagezi_spam_tlds_adblock.txt https://raw.githubusercontent.com/hagezi/dns-blocklists/main/adblock/spam-tlds.txt
        
        ls -lh hagezi_spam_tlds_adblock.txt
        if [ $(wc -c <"hagezi_spam_tlds_adblock.txt") -lt 1024 ]; then
          echo "❌ 下载失败：文件太小"
          exit 1
        fi

    # 3. 转换
    - name: Convert to SRS
      run: |
        echo "🔄 正在转换..."
        sing-box rule-set convert --type adguard --output hagezi_spam_tlds_adblock.srs hagezi_spam_tlds_adblock.txt
        
        ls -lh hagezi_spam_tlds_adblock.srs
        if [ $(wc -c <"hagezi_spam_tlds_adblock.srs") -lt 1024 ]; then
          echo "❌ 转换失败：生成了空文件"
          exit 1
        fi
        echo "✅ 转换成功"

    - name: Commit and Push
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add hagezi_spam_tlds_adblock.srs
        
        if git diff --cached --quiet; then
          echo "⚠️ 规则无变化"
        else
          git commit -m "Auto-update: HaGeZi Spam TLDs"
          git pull --rebase origin main
          git push
        fi
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Clean up
      run: |
        rm -f hagezi_spam_tlds_adblock.txt sing-box_1.11.4_linux_amd64.deb
