name: yokoffing's Privacy Essentials (Fixed)

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */12 * * *'

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  install-and-compile:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Install Sing-Box v1.11.4
      run: |
        wget https://github.com/SagerNet/sing-box/releases/download/v1.11.4/sing-box_1.11.4_linux_amd64.deb
        sudo dpkg -i sing-box_1.11.4_linux_amd64.deb

    - name: Download Filter
      run: |
        curl -fSL -o yokoffing_privacy_essentials.txt https://raw.githubusercontent.com/yokoffing/filterlists/refs/heads/main/privacy_essentials.txt
        
        # 只要文件不是空的就行
        ls -lh yokoffing_privacy_essentials.txt
        if [ $(wc -c <"yokoffing_privacy_essentials.txt") -lt 100 ]; then
          exit 1
        fi

    - name: Convert to SRS
      run: |
        sing-box rule-set convert --type adguard --output yokoffing_privacy_essentials.srs yokoffing_privacy_essentials.txt
        
        ls -lh yokoffing_privacy_essentials.srs
        srs_size=$(wc -c <"yokoffing_privacy_essentials.srs")
        
        # 【关键修改】把安全门槛从 1024 降到 500
        # 因为这个规则集编译后只有 1021 字节，是正常的
        if [ $srs_size -lt 500 ]; then
          echo "❌ 转换失败：生成了空文件 ($srs_size bytes)"
          exit 1
        fi
        echo "✅ 转换成功 (文件较小是正常的)"

    - name: Commit and Push
      run: |
        git config --global user.name "github-actions[bot]"
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git add yokoffing_privacy_essentials.srs
        
        if git diff --cached --quiet; then
          echo "No changes"
        else
          git commit -m "Auto-update: Yokoffing Privacy"
          git pull --rebase origin main
          git push
        fi
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Clean up
      run: |
        rm -f yokoffing_privacy_essentials.txt sing-box_1.11.4_linux_amd64.deb
